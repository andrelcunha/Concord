meta {
  name: Create Channel
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/channels/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "general",
    "server_id": {{serverId}}
  }
}
